package br.gov.frameworkdemoiselle.prodepa.queryfilter;


import org.junit.Test;

public class CacheParameterTest  {

  @Test
  public void testSingleResultById() {
   /* LoadingCache<Class, String> graphs = CacheBuilder.newBuilder()
            .maximumSize(1000)
            .expireAfterWrite(10, TimeUnit.MINUTES)
            //.removalListener(MY_LISTENER)
            .build(
                new CacheLoader<Class, String>() {
                  public Graph load(String key) throws AnyException {
                    return createExpensiveGraph(key);
                  }
                });*/
  }
  
  
  
}
